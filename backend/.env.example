# KisanBuddy Backend Environment Variables

# Google Gemini API (for crop diagnosis)
GEMINI_API_KEY=your-api-key-here


# AnthroKrishi Access (requires allowlisting)
ANTHROKRISHI_GWCID=your-google-workspace-customer-id
ANTHROKRISHI_DEV_EMAIL=developer@yourdomain.com

# Google Earth Engine
EARTH_ENGINE_SERVICE_ACCOUNT=/path/to/service-account.json

# CIB&RC RAG Index (Vertex AI Search)
CIBRC_RAG_INDEX=projects/your-project/locations/us-central1/indexes/your-index

# Server Configuration
PORT=8080
DEBUG=false

# JWT secret for signing access tokens
# REQUIRED: Must be set to a secure random value in production
# Generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# The default value 'please_change_this_secret' is rejected at startup
JWT_SECRET=please_change_this_secret

# Admin usernames (comma-separated list)
# Users with these usernames will have admin privileges (metrics, uploads)
ADMIN_USERNAMES=admin

# Weather API - Tomorrow.io (required for /api/weather_forecast)
# Falls back to free Open-Meteo service if not configured
TOMORROW_API_KEY=your-tomorrow-io-api-key

# Optional: data.gov.in integration
DATAGOV_API_KEY=
DATAGOV_RESOURCE_ID=

# Geoapify Places API (free tier). Set to enable nearby mandi search by coordinates.
GEOAPIFY_API_KEY=

# Optional: Render deploy keys (CI)
RENDER_API_KEY=
RENDER_SERVICE_ID=

# Serve exported frontend from backend if set to 1
SERVE_FRONTEND=0
